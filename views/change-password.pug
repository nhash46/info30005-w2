extends layout

block content
    .container
        #forum-title
            h1 #{title}
            form(method='POST', action='/user/change-password')
                #form-group
                    label Current Password
                    input.form-control(name='password', type='text')
                br
                #form-group
                    label New Password
                    input.form-control(name='new_password', type='text')
                br
                #form-group
                    label Confirm New Password
                    input.form-control(name='new_password2', type='text')
                br
                input.btn.btn-primary(type='submit', value='Submit')