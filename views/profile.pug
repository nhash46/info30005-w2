extends layout

block content
    #jumbotronConsultation
        .container
            .jumbotron.text-center#jumbotronProfile
                h2 Welcome #{user.first_name}
                a.btn.btn-primary.btn-sm(href='/user/profile/edit/'+user._id) Edit Profile
